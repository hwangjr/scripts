#!/bin/sh
cat >> /root/log.txt 2>&1  <<EOF
================== `date` =====================
`free -m`
================== `date` =====================
`ps afux`
================== `date` =====================
`top -b -n 1`
================== `date` =====================
`vmstat 1 5`
================== `date` =====================
`time /usr/sbin/tcpdump -s0 -A -n -i any -c 1000 | grep ^Host`
EOF
